#File Sharing and Web Portal#
to install,
1. git clone to /var/www/html
2. execute init_server.sh
3. import sql.sql
4. reset network
5. access via ip_add/tradoc-ftp
6. change the nameserver to a more suitable domain name